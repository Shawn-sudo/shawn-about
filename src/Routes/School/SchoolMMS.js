const SchoolMMS = () => {
  return (
    <div
      style={{
        fontWeight: "bolder",
        fontSize: "larger",
        padding: 20,
        backgroundColor: "#fdd835",
      }}
    >
      Developing
    </div>
  );
};

export default SchoolMMS;
